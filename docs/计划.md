# 计划

正文在 `docs/specs/看板/计划.md`（从工作台同步来的，那边是家）。契约在 `docs/specs/看板/契约.md`。
票在工作台 `docs/specs/看板/tickets/`。

这份文件只记**这个仓库落地时和计划不一样的地方**：

- **目录布局镜像工作台**（不是计划 Q31 写的 `opsmetrics/` + `web/`）：同步来的文件一个字不改、import 不用重写、测试原样能跑。
- **服务用标准库 wsgiref 不用 Flask**：零依赖，内网部署少一步；而且 2026-09-18 落地那台机器装不了 PyPI 的包，Flask 写了也验不了。
