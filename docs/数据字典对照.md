# 数据字典对照

左两列从 `docs/口径.md` 抄来（口径要的输入列，列名是工作台台账 / 报表里的原名）；右三列等公司数据字典拿到后填。
「判定」三选一：**能算**（字段在、语义同）/ **缺字段**（公司库里没有）/ **语义不同要重验**（有但口径不一样，重跑黄金用例）。

上游报表原始列名（和台账列名不同的）：`交易金额（美元）` → 台账 `交易金额`；`代理商用户ID` → `代理商ID`；网关表 `时间类别` → `报表类型`、`统计日期` → `统计周期`、`交易金额_美元` → `交易金额`（`churn/adapters.py`）。

| 口径 | 要的输入列 | 公司字典里的字段 | 判定（能算 / 缺字段 / 语义不同要重验） | 备注 |
|---|---|---|---|---|
| 1 名字清洗 | `直签人`（站点行） | | | 人名 / 19 位代理商 ID / 空 三种都会出现 |
| 1 名字清洗 | `所属BD`（出单监控报表） | | | 空格子要给空串不给 `nan`（§2.18.96） |
| 1 名字清洗 | `代理商名称` | | | 直签人是 ID 时显示「代理商：<名称>」 |
| 2 交易大盘 | 日期键（台账的天 = 报表 `统计周期`，`YYYY-MM-DD`） | | | 日界要核（见下） |
| 2 交易大盘 | `用户ID`（19 位文本） | | | 不经浮点 |
| 2 交易大盘 | `站点` | | | 站点数按 (用户ID, 站点) 去重 |
| 2 交易大盘 | `商户名称` | | | 排名按用户ID 合并站点 |
| 2 交易大盘 | `直签人` | | | 构成维度 |
| 2 交易大盘 | `接入模式` | | | 构成维度；空 → 「—」 |
| 2 交易大盘 | `交易金额`（美元，浮点） | | | |
| 2 交易大盘 | `交易笔数`（整数） | | | 「有交易的站点」只数笔数 > 0 |
| 3 周期分桶 | 同第 2 节全部列 | | | |
| 3 周期分桶 | `代理商名称` | | | 维度「代理商」，空 → 「直签」 |
| 3 周期分桶 | 周台账：`期次`（如 `2026 W37 (2026-09-04~2026-09-10)`）、`start`、`end`、`complete`、`rows` | | | 周界从期次标签读；只做校验不做数据源 |
| 4 流失状态机 | `用户ID`、`站点`（两者空则整行不算） | | | 单位是站点 |
| 4 流失状态机 | `交易金额`、`交易笔数` | | | 「有交易」= 笔数 > 0；掉量用金额 |
| 4 流失状态机 | `商户名称`、`直签人`、`代理商ID`、`代理商名称`、`接入模式` | | | 只带出，不参与算数 |
| 4 流失状态机 | 网关台账：日期键（`统计日期`）、`网关`、`用户ID`、`交易笔数` | | | 全公司收单、无站点列；缺当天 → 「网关数据缺」 |
| 5 日报汇总 | 第 4 节的输出（`hits[].type / tpv30 / drop.base / 用户ID`） | | | 派生，不直接读表 |
| 6 老板周报 | 周台账 `rows`：`用户ID`、`站点`、`商户名称`、`直签人`、`代理商名称`、`交易金额`、`交易笔数` | | | 「有交易」= 笔数 > 0 或金额 > 0 |
| 6 老板周报 | 每日状态（第 4 节输出）`hits[].type / tpv30` | | | 在途风险 |
| 6 老板周报 | 多维表格回读：`跟进人`、`状态`（未跟进 / 跟进中 / 已挽回 / 放弃） | | | 人手填；判「有人跟」看状态 |
| 7 出单监控 | `用户ID` | | | |
| 7 出单监控 | `站点` | | | 规整：小写、去协议 / `www.` / 尾 `/` |
| 7 出单监控 | `商户名称`、`所属BD` | | | 只进通知 |
| 7 出单监控 | `累计TPV_USD` | | | $100 线 |
| 7 出单监控 | `第一笔成功交易时间` | | | |
| 7 出单监控 | `站点_商户提交时间` | | | 老商户排除 |
| 7 出单监控 | `提交通道时间` | | | |
| 7 出单监控 | `通道结果反馈时间` | | | 开通日 / 开通天数 |
| 7 出单监控·耗时 | `站点_上线时长`、`支付方式_上线时长`、`站点_网站审核耗时`、`支付方式_网站审核耗时`、`通道审核耗时`、`集成耗时` | | | 小时 · 不含节假日；`0` 有四种意思（§2.18） |
| 7 出单监控·耗时 | `站点_风控审核时间`、`支付方式_商户提交时间`、`支付方式_风控审核时间` | | | 六列耗时的起 / 终点 |
| 7 出单监控 | `建议进件通道` | | | 分组维度（§2.18.2） |
| 7 出单监控 | 第三张 sheet「站点审核失败」：`用户ID`、`站点`、`所属BD` | | | 按名字认 sheet（§2.20） |
| 8 生命周期漏斗 | `用户ID`、`站点`、`站点_商户提交日期`、`通道结果反馈日期`、`第一笔成功交易日期` | | | 自然日；来自出单监控台账「明细」 |
| 8 生命周期漏斗 | 第 4 节输出 `sites[key].silent_days` | | | 后两级 |
| 9 转化率（JS） | 转化率报表：来源、指标名、期次、各环节比率与分母（列名见 `static/js/conversion/README.md` / `dataset.js`） | | | （待核：列名清单没在本文档展开） |
| 9 商户成功率（JS） | 交易流水：状态、失败编码、失败文案、金额、币种、卡 BIN 国家、IP 国家等（列映射在 `static/js/merchant/config.js`） | | | （待核：同上） |

## 已知的口径冲突（从公司看板截图看出来的，等字典再核）

抄自 `docs/specs/看板/计划.md` 末尾：

1. **周界**：公司看板的周从**周日**起（09-17 周四显示「本周已过 5/7 天」）；工作台的周是**周五 → 周四**、跟报表走，周号按结束日算（`§2.17.7`）。两个「本周 TPV」摆一起必然对不上。`churn/trade.py: build(anchor=…)` 已参数化，过渡期并存；改公司口径的事另谈。
2. **日界**：公司看板是「UTC+8 自然日」；工作台的一天是报表 `统计周期` 给的日期，口径层不做时区换算。要核两边的「今天」是不是同一段时间。
3. **站点 vs 商户**：公司看板「商户与代理商」是**商户级**；工作台流失的单位是**站点**（用户ID + 站点），交易大盘的排名才是商户级（合并站点）。一家七个站点在两边是 1 条和 7 条。
4. **映射管理 vs `agent_owner` / `bd_emails`**：公司看板的「映射管理」和工作台的直签人 / 代理商 / 未分配BD（`names.py`）、代理商ID → BD 映射（`churn.agent_owner`）、BD 姓名 → 邮箱（`order_monitor.bd_emails`）是同一件事的两份，要定谁为准。

要去问的五件事（同一份计划）：① 技术栈 ② 有没有站点级 / 商户级明细接口 ③ 映射管理谁为准 ④ 数据字典 ⑤ 改周口径找谁谈。
